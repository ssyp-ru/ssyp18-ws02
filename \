#include "feature.h"
#include "common.h"
#include "hero.h"
#include <ncruses.h>

void pick_up(actor_t * actor, feature_t feature){
	int amount = feature.inventory->amount;
	int item_count = actor->inventory
	int x = feature.x;
	int y = feature.y;
	for(int i = 0; i < amount; i++){
	

	}
}

void throw_away(item_t * item, level_t * level, int x, int y){

}


